package duke.parser;

/**
 * This enum ensures that all commands are categorized into an appropriate command type.
 */
public enum CommandType {
    LIST, DELETE, TODO, DEADLINE, TRADITIONAL_TASK, EVENT, FIND, DONE, BYE
}
